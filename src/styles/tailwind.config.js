// tailwind.config.js
module.exports = {
    darkMode: 'class', // Enable dark mode with a class
    theme: {
      extend: {
        // You can extend the theme here if needed
      },
    },
    plugins: [],
  };
  